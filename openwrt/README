This is an OpenWrt package feed containing the olsrd-git package.

To use this package, add the following line to the feeds.conf
in the OpenWrt buildroot:

  src-git olsrd http://olsr.org/git/olsrd.git
  
Update the feed:

  ./scripts/feeds update olsrd
  
Activate the package:

  ./scripts/feeds install olsrd-git
  
The "olsrd-git" package should now appear in menuconfig.

